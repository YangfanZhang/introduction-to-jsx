import React from "react";
import ReactDOM from "react-dom";

//1, what to show, 2 where to show, 3when to complete
ReactDOM.render(
  <div>
    <h1> Hello</h1>
  </div>,
  document.getElementById("root")
);
